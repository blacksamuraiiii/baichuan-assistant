# 百川数据助手依赖包列表
# 使用 pip install -r requirements.txt 安装所有依赖

# 核心依赖
requests>=2.25.0          # 降低版本要求，提升兼容性
pandas>=1.3.0             # 降低版本要求
openpyxl>=3.0.0

# 加密依赖
cryptography>=3.3.0       # 降低版本要求

# GUI界面
customtkinter>=5.0.0
CTkMessagebox>=2.0.0

# 日期处理（可选依赖，主要功能用标准库）
python-dateutil>=2.8.0

# 打包程序
pyinstaller>=4.8

# 可选优化：性能提升相关依赖
# numpy>=1.20.0           # pandas加速（可选）
# psutil>=5.8.0           # 系统监控（可选）